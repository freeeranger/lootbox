{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Lootbox window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
